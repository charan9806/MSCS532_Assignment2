# Assignment 2 — Divide-and-Conquer Algorithms (Quick Sort vs Merge Sort)

## What’s inside
- `src/sorts.py`: Quick Sort (random pivot) + Merge Sort implementations
- `src/benchmark.py`: benchmarks across sorted/reverse/random datasets
- `results/`: CSV outputs + plots

## How to run
```bash
cd src
python benchmark.py
